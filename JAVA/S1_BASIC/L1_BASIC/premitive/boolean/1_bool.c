#include <stdio.h>
#include <stdbool.h>

int main() {
    bool isReady = true;   // can be true or false
    if (isReady) {
        printf("Ready!\n");
    } else {
        printf("Not ready!\n");
    }
    return 0;
}